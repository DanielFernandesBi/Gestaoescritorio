import { EmBreve } from "@/components/EmBreve";

export default function Page() {
  return (
    <EmBreve
      eyebrow="Designações"
      titulo="Audiências"
      descricao="Instrução, custódia, júri e sessões — presencial, vídeo ou híbrida."
    />
  );
}
