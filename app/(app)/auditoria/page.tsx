import { EmBreve } from "@/components/EmBreve";

export default function Page() {
  return (
    <EmBreve
      eyebrow="Log imutável · prova"
      titulo="Auditoria"
      descricao="Toda operação registrada. Relatório de agente não é prova; a auditoria é."
    />
  );
}
