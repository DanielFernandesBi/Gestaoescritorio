import { EmBreve } from "@/components/EmBreve";

export default function Page() {
  return (
    <EmBreve
      eyebrow="Manual vivo · evolução"
      titulo="Sistema & evolução"
      descricao="Sugestões de schema e migrações autorizadas."
    />
  );
}
