import { EmBreve } from "@/components/EmBreve";

export default function Page() {
  return (
    <EmBreve
      eyebrow="Gate de validação humana"
      titulo="Validação de Daniel"
      descricao="Prazos e audiências de automação aguardando confirmação."
    />
  );
}
